spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pets?useSSL=false&serverTimezone=Asia/Seoul&useLegacyDatetimeCode=false&charset=utf8
    username: root
    password: 1257
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL57Dialect